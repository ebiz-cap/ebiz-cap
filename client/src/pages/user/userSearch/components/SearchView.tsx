import styled from "styled-components";
const SearchView = (): JSX.Element => {
  return <SearchViewContainer>yes</SearchViewContainer>;
};

const SearchViewContainer = styled.div`
  width: 100%;
`;
export default SearchView;
